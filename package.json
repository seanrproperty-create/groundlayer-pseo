{
  "name": "groundlayer-pseo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:schema": "wrangler d1 execute groundlayer-pseo --remote --file=./schema.sql",
    "d1:schema:local": "wrangler d1 execute groundlayer-pseo --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
